# Emot-ID v0.1.2

Corrective patch for compact Romanian appearance-control spacing discovered by the `v0.1.1`
release workflow.

## Changes

- Preserves at least 8px of rendered inline space around `Luminos` and `Întunecat` at compact and
  desktop widths across Chromium and WebKit font metrics.
- Keeps the stricter geometry regression contract unchanged.

Romanian voice, English behavior, emotion models, safety gating, persistence, privacy, and the
external Google AI Mode handoff are unchanged from `v0.1.1`.

## Verification

- Lint, unit/integration, acceptance, bilingual and psychological-copy audits, TypeScript,
  production build, and budgets: 86 files / 685 tests, pass.
- Mobile Safari and Mobile Chrome Playwright matrix: 258/258, pass.
- Production PWA lifecycle and mobile performance probes: pass.

## Evidence Limits

The external-evidence waivers documented in `v0.1.1` remain unchanged.
